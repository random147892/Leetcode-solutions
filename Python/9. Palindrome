class Solution:
    def isPalindrome(self, x: int) -> bool:
        for digit in str(x):
            for digit2 in str(x)[::-1]:
                if digit == digit2:
                    return True
                else:
                    return False
